using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveUpDown : MonoBehaviour
{
    // Start is called before the first frame update

    //public float speed = 2.5f;

    // Update is called once per frame

    void Start ()
    {
        //transform.Translate(new Vector3(0f, 0f, 0f), Space.Self);
    }
    void Update()
    {
        //transform.position = new Vector3 (Mathf.PingPong (Time.time*speed,5), transform.position.y, transform.position.z, Space.S);
        
        //transform.Translate(Vector3.up * JumpSpeed) Space.Self);
        //transform.Translate(0f * Time.deltaTime, 0.1f * Time.deltaTime, 0, Space.Self);
    }
}
